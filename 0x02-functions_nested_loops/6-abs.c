#include "main.h"

/**
 * _abs(int i)- checks for the absolute value
 * @i : a int variable
 * Return: i if positive, else return i multiplied by -1 if negative
 *
 */
int _abs(int i)
{
if (i >= 0)
{
return (i);
}
else
{
return (i * -1);
}
}
